import './HomePage.css'

/**
 * Home route shell — content removed for a fresh build (Discover flow lives in PageShell).
 */
export function HomePage() {
  return <main className="home-empty" id="home-main" aria-label="Home" />
}
